# WebADB Demo

Demo of `@yume-chan/adb` and `@yume-chan/adb-backend-web`.

## Start

From root folder:

```sh
npm run start:demo
```

From this folder:

```sh
npm start
```

Then navigate to `http://localhost:9000`.
