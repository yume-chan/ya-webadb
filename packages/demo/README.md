# WebADB Demo

## Start

In `demo` folder:

```sh
npm start
```

Or in root folder:

```sh
npm run start:demo
```

## Watch changes in `webadb`

From another shell, run in root folder:

```sh
npm run build:watch
```
