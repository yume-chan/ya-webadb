import Struct from './index';

describe('Struct', () => {
    describe("Index", () => {
        it('should export default Struct', () => {
            expect(Struct).toBeDefined();
        });
    });
});
